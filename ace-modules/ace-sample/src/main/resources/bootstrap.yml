spring:
  application:
    name: ace-sample
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_SERVER_ADDR:127.0.0.1:8848}
        file-extension: yaml
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true